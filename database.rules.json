{
    "rules": {
        "utilisateurs": {
            "$uid": {
                ".read": true,
                ".write": "auth.uid === $uid",
                ".validate": "newData.exists()"
            }
        },
        ".read": true
    }
}
